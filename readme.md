# Expense Tracker / DBMS EL Project

This project is a full-stack expense tracking application.

## 📂 Project Structure

- **`/backend`**: Node.js & Express API server.
- **`/frontend`**: React + Vite frontend application.
- **`/docs`**: Project documentation, reports, and diagrams.
  - **`/docs/reports`**: PDF and DOCX submissions.
  - **`/docs/images`**: Diagrams (ERD, Schema, etc.).
- **`/report`**: Scripts for generating report documents.

## 🚀 Getting Started

### Prerequisites
- Node.js installed
- Database (MongoDB/MySQL)

### Setup

1.  **Backend**:
    ```bash
    cd backend
    npm install
    # Create .env file here
    npm start
    ```

2.  **Frontend**:
    ```bash
    cd frontend
    npm install
    npm run dev
    ```
